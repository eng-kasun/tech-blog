---
interface Props {
  title: string;
  description: string;
  date: string;
  category: string;
  slug: string;
  image?: string;
  featured?: boolean;
}

const { title, description, date, category, slug, image, featured = false } = Astro.props;

const formattedDate = new Date(date).toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'short',
  day: 'numeric',
});
---

<article class:list={[
  'group',
  featured ? 'md:col-span-2 md:row-span-2' : '',
]}>
  <a href={`/tech-blog/blog/${slug}/`} class="block h-full">
    {/* Featured articles get a big image */}
    {image && featured && (
      <div class="mb-3 overflow-hidden border border-ink-200">
        <img
          src={image}
          alt={title}
          class="w-full h-48 md:h-64 object-cover grayscale hover:grayscale-0 transition-all duration-500"
          loading="lazy"
        />
      </div>
    )}

    {/* Category & date line */}
    <div class="flex items-center gap-2 mb-2">
      <span class="text-[10px] font-sans font-bold uppercase tracking-[0.2em] text-accent">
        {category.replace(/-/g, ' ')}
      </span>
      <span class="text-ink-200">|</span>
      <time class="text-ink-300 text-[11px] font-sans">{formattedDate}</time>
    </div>

    {/* Headline */}
    <h3 class:list={[
      'font-headline font-bold text-ink-800 group-hover:text-accent transition-colors leading-tight mb-2',
      featured ? 'text-2xl md:text-3xl' : 'text-lg',
    ]}>
      {title}
    </h3>

    {/* Description — newspaper lead paragraph style */}
    <p class:list={[
      'text-ink-500 font-body leading-relaxed',
      featured ? 'text-base' : 'text-sm line-clamp-3',
    ]}>
      {description}
    </p>

    {/* Thin rule at bottom */}
    <div class="mt-4 border-b border-ink-200 border-dotted"></div>
  </a>
</article>
